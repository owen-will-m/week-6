     export default class Blade {
          constructor(x, y, rot, height, width, deathFrame) {
               this.x = x;
               this.y = y;
               this.rot = rot;
               this.height = height;
               this.width = width;
               this.deathFrame = deathFrame;
          }
          draw(frame) {

          }
     }